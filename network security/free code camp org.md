history command give older commands 
**history | grep something** useful trick


**Commands**:


**grep** return whole line in a file
**cut** can be used specific from each line of a file or input stream
**tr** it cases delete or translates character in a string


**Python**
zip combines multiple iterable such as lists, tuples, string, dict into a single iterater of tuples


**join() method**:

syntax: ```
```python
separator.join(iterable)
```

all element in the iterable must be string other wise typeerror will occur

```python
words = ["Python", "is", "fun"]
    result = " ".join(words)
    print(result)
    # Output: Python is fun
```

string method

string.replace("string", "string")
string.find("string") # find first index

